<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows plain-HTTP (cleartext) traffic ONLY to local development hosts so the
  demo app can reach a locally-running FastAPI backend from the Android emulator
  (10.0.2.2 is the emulator's alias for the host machine's loopback). All other
  traffic still requires HTTPS, so this does not weaken production security.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
